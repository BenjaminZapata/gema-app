version: '3.8'

services:
  next-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: nextjs-ssr-prod:latest
    ports:
      - "3000:3000" # Mapea el puerto 3000 de tu PC de producción al 3000 del contenedor
    restart: always
    environment:
      - NODE_ENV=production